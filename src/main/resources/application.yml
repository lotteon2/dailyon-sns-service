server:
  port: 8088
spring:
  config:
    activate:
      on-profile: local,dev,prod
  application:
    name: sns-service
  # config service settings
  cloud:
    config:
      name: sns-service
management:
  endpoints:
    web:
      exposure:
        include: health, info, refresh, busrefresh
